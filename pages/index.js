// Once the requrie of Minio is removed, the error will be shown
// const Minio = require('minio')

// These modules were not found:

// * fs in ./node_modules/mkdirp/index.js, ./node_modules/minio/dist/main/minio.js

// To install it, you can run: npm install --save fs

export default () => (
    <div>
        <h1>Hello Next.js</h1>
    </div>
)